type ContextEntry {
    tokens: string[],
    _text: string, // Original text for reference (kept for potential future use)
}

class NarrowMind {
    tokens:string[]
    
    constructor() {
        this.tokens = [];

    }

    train(data:string) {
        
    }
}

module.exports =  NarrowMind;